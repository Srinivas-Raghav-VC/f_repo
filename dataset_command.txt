# 0) Install
pip install -U datasets tqdm

# 1) Set your HF token so streaming works fast (optional but recommended)
export HUGGINGFACE_TOKEN=hf_xxx

# 2) Run (defaults create ~2k Hindi, ~2k English, ~1k mixed, ~500 each ur/pa/bn, ~300 adversarial)
python build_mmie_datasets.py --out_dir data

# Bigger run (10k+ sized sets)
python build_mmie_datasets.py --out_dir data \
  --n_forget 10000 --n_retain 10000 --n_mixed 4000 \
  --n_xlang 2000 --n_adv 1000
